name: shuju-monorepo-snapshot

on:
    push:
        branches:
            - "main"

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: "pages"
    cancel-in-progress: false

jobs:
    deploy-snapshot:
        uses: ./.github/workflows/maven-deploy-snapshot.yml
        secrets: inherit

    deploy-snapshot:
        needs: [deploy-prod]
        uses: ./.github/workflows/maven-deploy-doc.yml
        with:
            version: ${{ github.event.release.tag_name }}
